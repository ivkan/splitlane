[package]
name = "splitlane-terminal-ghostty"
version.workspace = true
edition = "2024"
rust-version.workspace = true
license.workspace = true
publish = false

[features]
default = []
native = ["dep:splitlane-libghostty-sys", "splitlane-libghostty-sys/link"]

[dependencies]
base64 = "0.22"
regex = "1"
serde_json.workspace = true
thiserror.workspace = true

[target.'cfg(any(target_os = "linux", target_os = "windows"))'.dependencies]
splitlane-libghostty-sys = { path = "../splitlane-libghostty-sys", optional = true }

[lints]
workspace = true
