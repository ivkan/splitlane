<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <!--
    Declares an explicit execution level so Windows does NOT apply its
    installer-detection heuristic to this crate's test binary. That heuristic
    auto-elevates (UAC) any unmanifested .exe whose name contains
    "install"/"setup"/"update"; the unit-test harness is
    splitlane_mcp_install-<hash>.exe, which would otherwise fail to launch under
    `cargo test` with os error 740 ("the requested operation requires
    elevation"). `asInvoker` runs with the caller's token - no elevation.
  -->
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
    <security>
      <requestedPrivileges>
        <requestedExecutionLevel level="asInvoker" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>
</assembly>
