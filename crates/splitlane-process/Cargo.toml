[package]
name = "splitlane-process"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Bounded external-process execution (wall-clock deadline + stdout cap) shared across Splitlane crates"

# Zero external dependencies on purpose: this crate is a dependency of the
# embedded `splitlane-shim`, so anything pulled in here inflates the shim binary
# that ships inside the main executable.

[lints]
workspace = true
