# Demo task for `review-pipeline.flow.toml`

This is a safe, read-only task used by the committed flow example.

Inspect `../README.md` and `../docs/user/scripting.md`, then report one concrete
documentation drift risk that could confuse a new Splitlane user. Do not modify
files. When finished, print exactly one final line that starts with `DONE:`
followed by a one-sentence summary.
